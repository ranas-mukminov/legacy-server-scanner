# Legacy Migration Assistant 🔄

![CI](https://github.com/ranas-mukminov/legacy-migration-assistant/actions/workflows/ci.yml/badge.svg)
![License](https://img.shields.io/badge/license-Apache%202.0-blue.svg)
![Python](https://img.shields.io/badge/python-3.10%2B-blue.svg)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

## Описание

**Legacy Migration Assistant** — это набор инструментов командной строки для аудита легаси-серверов Linux и автоматической генерации Docker Compose и Kubernetes-манифестов. Инструмент сканирует установленные пакеты, запущенные сервисы, открытые порты, cron-задания и конфигурационные файлы, затем строит карту топологии приложения и генерирует готовые к развёртыванию файлы с безопасными настройками по умолчанию.

Этот инструмент предназначен для DevOps-инженеров, системных администраторов и фриланс-интеграторов, которым необходимо контейнеризировать и мигрировать легаси-инфраструктуру на современные платформы, такие как Docker, K3s или Kubernetes, без ручного документирования каждого компонента.

## Основные возможности

- **Автоматическая инвентаризация сервера** — сканирование пакетов, systemd-сервисов, портов, cron-заданий и конфигов
- **Построение карты топологии приложения** — автоматическое выявление зависимостей между компонентами
- **Генерация Docker Compose** — создание готового `docker-compose.yaml` из карты топологии
- **Генерация Kubernetes-манифестов** — создание Deployment, Service, Ingress, PVC с настройками безопасности
- **Безопасные настройки по умолчанию** — автоматическое применение ограничений ресурсов, probes и `securityContext`
- **Сканирование в режиме read-only** — нет изменений на легаси-сервере во время сканирования
- **Расширяемая архитектура** — поддержка пользовательских классификаторов и AI-assisted рефайнмента топологии

## Архитектура и компоненты

Инструментарий состоит из двух основных CLI-утилит:

1. **`legacy-scan`** — сканер сервера, который собирает сырые данные и генерирует карты топологии и Docker Compose файлы
   - **Модули сканирования**: packages, services, ports, cron, configs
   - **Построение топологии**: анализ зависимостей и построение графа компонентов
   - **Генератор Compose**: создание Docker Compose YAML из топологии

2. **`legacy-k8s`** — генератор Kubernetes-манифестов, который преобразует карты приложений или Docker Compose в K8s-ресурсы
   - **Парсер Compose**: чтение Docker Compose и извлечение определений сервисов
   - **Генератор K8s**: создание Deployment, Service, Ingress, PVC манифестов
   - **Advisor ресурсов**: назначение CPU/memory requests и limits
   - **Политики безопасности**: внедрение best-practice `securityContext`, probes и network policies

**Поток данных:**

```
Легаси-сервер → [legacy-scan scan] → scan.json
scan.json → [legacy-scan map] → app-map.yaml
app-map.yaml → [legacy-scan compose] → docker-compose.yaml
docker-compose.yaml → [legacy-k8s from-compose] → K8s manifests/
```

## Требования

### Операционная система
- **Дистрибутивы Linux**: Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux, AlmaLinux или аналогичные
- **Версия Python**: 3.10 или выше

### Привилегии
- **Сканер** (`legacy-scan`): root или sudo для полной инвентаризации системы (рекомендуется)
- **Генератор** (`legacy-k8s`): не требует повышенных привилегий

### Дисковое пространство
- Минимум: ~50 МБ для установки
- Результаты сканирования: обычно 1–10 МБ на сервер

### Сеть
- Требуется доступ в Интернет для установки через pip
- Во время сканирования внешние подключения не используются (работает офлайн после установки)

## Быстрый старт

1. **Клонируйте репозиторий и установите**:
   ```bash
   git clone https://github.com/ranas-mukminov/legacy-migration-assistant.git
   cd legacy-migration-assistant
   pip install .
   ```

2. **Просканируйте легаси-сервер**:
   ```bash
   sudo legacy-scan scan --output scan.json
   legacy-scan map --scan scan.json --output app-map.yaml
   legacy-scan compose --map app-map.yaml --output docker-compose.yaml
   ```

3. **Сгенерируйте Kubernetes-манифесты** (на рабочей станции):
   ```bash
   legacy-k8s from-compose --compose docker-compose.yaml --output-dir k8s/ --namespace production --ingress-host example.com
   ```

4. **Проверьте и разверните**:
   - Внимательно проверьте `docker-compose.yaml` и `k8s/*.yaml`
   - Замените placeholder-секреты и переменные окружения
   - Протестируйте в локальном или staging-окружении перед продакшеном

## Подробная установка

### Установка из исходников (рекомендуется)

1. **Убедитесь, что установлен Python 3.10+**:
   ```bash
   python3 --version  # Должна быть версия 3.10 или выше
   ```

2. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/ranas-mukminov/legacy-migration-assistant.git
   cd legacy-migration-assistant
   ```

3. **Установите пакет**:
   ```bash
   pip install .
   ```

4. **Проверьте установку**:
   ```bash
   legacy-scan --help
   legacy-k8s --help
   ```

### Установка с зависимостями для разработки

Для контрибьюторов или тех, кто хочет запускать тесты и линтеры:

```bash
pip install -e .[dev]
```

Это установит дополнительные инструменты: `pytest`, `ruff`, `mypy`, `bandit`, `yamllint` и `pip-audit`.

## Настройка

### Конфигурация сканера

Инструмент `legacy-scan` не требует конфигурационного файла. Все опции передаются через аргументы командной строки:

```bash
legacy-scan scan --output scan.json
legacy-scan map --scan scan.json --output app-map.yaml
legacy-scan compose --map app-map.yaml --output docker-compose.yaml
```

### Конфигурация генератора Kubernetes

Инструмент `legacy-k8s` принимает следующие опции:

```bash
legacy-k8s from-compose \
  --compose docker-compose.yaml \
  --output-dir k8s/ \
  --namespace <ВАШ_NAMESPACE> \
  --ingress-host <ВАШ_ДОМЕН>
```

**Переменные окружения** в настоящее время не используются для конфигурации, но секреты должны управляться внешне (например, Kubernetes Secrets, HashiCorp Vault).

### Пример карты приложения

Карты приложений — это YAML-файлы, описывающие компоненты и их связи. Пример:

```yaml
components:
  - name: web
    component_type: web
    ports: [80, 443]
    volumes: [/var/www/html]
    environment: {}
    depends_on: [db]
    notes:
      - nginx detected
  - name: db
    component_type: database
    ports: [3306]
    volumes: [/var/lib/mysql]
    environment: {}
    depends_on: []
    notes:
      - mysql storage
relations:
  - source: web
    target: db
    description: web connects to db
packages: []
services: []
ports: []
cron: []
configs: []
```

## Использование и типовые задачи

### Сканирование легаси-сервера

1. **Войдите на легаси-сервер** (SSH или прямой доступ)
2. **Установите инструмент** (см. [Подробная установка](#подробная-установка))
3. **Запустите сканер**:
   ```bash
   sudo legacy-scan scan --output scan.json
   ```
   Это создаст файл `scan.json` с пакетами, сервисами, портами, cron и конфигами.

### Построение карты приложения

```bash
legacy-scan map --scan scan.json --output app-map.yaml
```

Это создаст структурированный YAML-файл с компонентами, зависимостями и примечаниями.

### Генерация Docker Compose

```bash
legacy-scan compose --map app-map.yaml --output docker-compose.yaml
```

Проверьте сгенерированный `docker-compose.yaml` и настройте теги образов, секреты и переменные окружения.

### Генерация Kubernetes-манифестов

```bash
legacy-k8s from-compose --compose docker-compose.yaml --output-dir k8s/ --namespace production --ingress-host <ВАШ_ДОМЕН>
```

Это создаст Deployment, Service, PersistentVolumeClaim и Ingress манифесты в директории `k8s/`.

### Кастомизация сгенерированных манифестов

- **Секреты**: замените placeholder-значения на Kubernetes Secrets или ссылки на переменные окружения
- **Лимиты ресурсов**: настройте CPU и memory requests/limits на основе реальной нагрузки
- **Probes**: уточните `livenessProbe` и `readinessProbe` для вашего приложения
- **Хранилище**: настройте StorageClass и размеры томов для PersistentVolumeClaims

## Обновление

Чтобы обновить инструмент до последней версии:

```bash
cd legacy-migration-assistant
git pull origin main
pip install --upgrade .
```

Если вы установили с флагом `-e` (editable mode), просто получите последние изменения:

```bash
git pull origin main
```

### Критические изменения

Проверьте `CHANGELOG.md` для версионных breaking changes. Инструмент находится в версии **0.1.0** (начальный scaffold), поэтому API может измениться в будущих релизах.

## Логи и устранение неполадок

### Логи

- **Логи сканера**: инструмент выводит данные в `stdout` и `stderr`. При необходимости перенаправьте в файл:
  ```bash
  sudo legacy-scan scan --output scan.json > scanner.log 2>&1
  ```

- **Сгенерированные манифесты**: проверяйте YAML-файлы напрямую; инструменты сами по себе не генерируют runtime-логи.

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| `Permission denied` при сканировании | Запустите `legacy-scan scan` с `sudo` или от root |
| `Command not found: legacy-scan` | Убедитесь, что `pip install .` завершился успешно и `~/.local/bin` в вашем `PATH` |
| Отсутствуют пакеты в выводе сканера | Некоторые пакеты могут не обнаруживаться, если установлены вне стандартных пакетных менеджеров |
| Сервисы Docker Compose без зависимостей | Вручную проверьте и отредактируйте `depends_on` в `docker-compose.yaml` |
| Kubernetes-манифесты без Ingress | Укажите флаг `--ingress-host` при запуске `legacy-k8s from-compose` |
| Устаревшая версия Python | Установите Python 3.10+ через пакетный менеджер вашего дистрибутива или pyenv |

### Отладка

Включите подробный вывод (если реализовано в будущих версиях):

```bash
legacy-scan scan --output scan.json --verbose
```

Сейчас проверяйте промежуточные файлы (`scan.json`, `app-map.yaml`) для диагностики проблем.

## Замечания по безопасности

### Общие практики безопасности

- **Меняйте дефолтные пароли**: никогда не используйте placeholder-пароли в продакшене
- **Защищайте секреты**: используйте Kubernetes Secrets, HashiCorp Vault или аналогичные инструменты
- **Ограничивайте доступ**: не выставляйте порты 22, 3306 и другие чувствительные порты напрямую в Интернет
- **Используйте TLS**: включайте TLS/SSL для веб-сервисов и баз данных
- **Network policies**: применяйте Kubernetes NetworkPolicies для ограничения трафика pod-to-pod

### Безопасность сканера

- Сканер **не изменяет** легаси-сервер; он только читает состояние системы
- **Учётные данные не логируются** в вывод сканирования; однако пути к конфигурационным файлам записываются
- Проверяйте `scan.json` и `app-map.yaml` перед передачей, чтобы убедиться в отсутствии утечки чувствительных данных

### Безопасность Kubernetes

Сгенерированные манифесты включают:

- `securityContext` с `runAsNonRoot: true` и `allowPrivilegeEscalation: false`
- `readOnlyRootFilesystem: true` где применимо
- Resource requests и limits для предотвращения неконтролируемого роста контейнеров
- Health проверки для обеспечения доступности приложения

**Всегда проверяйте и тестируйте манифесты в staging-окружении перед развёртыванием в продакшене.**

## Структура проекта

```
legacy-migration-assistant/
├── src/
│   └── legacy_migration_assistant/
│       ├── legacy_server_scanner/     # Модули сканера
│       ├── legacy_to_k8s_blueprints/  # Модули генератора K8s
│       ├── router_policy_to_config/   # Генератор конфигов роутеров (бонус)
│       └── core/                       # Общие модели
├── tests/                              # Unit и integration тесты
├── examples/                           # Примеры YAML-файлов
│   ├── maps/                          # Примеры топологии приложений
│   ├── compose/                       # Примеры Docker Compose
│   └── k8s/                           # Примеры Kubernetes манифестов
├── scripts/                            # Скрипты lint, security, performance
├── pyproject.toml                      # Конфигурация Python-пакета
├── CHANGELOG.md                        # История версий
├── CONTRIBUTING.md                     # Руководство по контрибуции
├── LEGAL.md                            # Юридические оговорки и условия использования
└── LICENSE                             # Лицензия Apache 2.0
```

## План развития

### Планируемые функции

- **AI-assisted рефайнмент топологии**: предложение типов компонентов и зависимостей с использованием LLM
- **Поддержка дополнительных платформ**: FreeBSD, Alpine Linux
- **Расширенное сканирование безопасности**: обнаружение устаревших пакетов и CVE
- **Оркестрация нескольких серверов**: сканирование нескольких серверов и объединение карт топологии
- **Prometheus exporter**: экспорт результатов сканирования как метрик Prometheus
- **Grafana дашборд**: визуализация инвентаризации легаси-инфраструктуры

### Известные ограничения

- В настоящее время поддерживаются дистрибутивы на основе systemd (Ubuntu, Debian, RHEL и т.д.)
- SysVinit и upstart сервисы пока не полностью поддерживаются
- Функции policy для роутеров экспериментальные и могут измениться

См. [GitHub Issues](https://github.com/ranas-mukminov/legacy-migration-assistant/issues) для активных запросов функций и отчётов об ошибках.

## Как внести вклад

Вклад приветствуется! Пожалуйста, следуйте этим рекомендациям:

1. **Откройте issue** для обсуждения значительных изменений перед началом работы
2. **Держите PR сфокусированными** и включайте тесты где уместно
3. **Используйте type hints** и следуйте PEP 8 стилю
4. **Запустите линтеры** перед отправкой:
   ```bash
   bash scripts/lint.sh
   pytest
   ```

### Настройка окружения для разработки

```bash
git clone https://github.com/ranas-mukminov/legacy-migration-assistant.git
cd legacy-migration-assistant
pip install -e .[dev]
```

### Стиль кода

- **Линтер**: Ruff (ограничение 100 символов)
- **Type checker**: mypy
- **Тестирование**: pytest
- **CLI framework**: argparse

См. [CONTRIBUTING.md](CONTRIBUTING.md) для деталей.

## Лицензия

Этот проект лицензирован под **Apache License 2.0**. См. [LICENSE](LICENSE) для полных условий.

## Автор и коммерческая поддержка

**Автор**: [Ranas Mukminov](https://github.com/ranas-mukminov)

Для внедрения в продакшене, аудита инфраструктуры, консультаций по миграции и постоянной поддержки DevOps/SRE посетите **[run-as-daemon.ru](https://run-as-daemon.ru)** или свяжитесь с автором через профиль GitHub.

Коммерческие услуги включают:

- Аудит легаси-серверов и планирование миграции
- Проектирование инфраструктуры Docker и Kubernetes
- Настройка CI/CD конвейеров и автоматизация
- Решения для мониторинга, логирования и алертинга (Prometheus, Grafana, Loki)
- Усиление безопасности и консультации по соответствию требованиям

---

**Всегда проверяйте, тестируйте и валидируйте конфигурации в безопасной среде перед применением в продакшене.**
